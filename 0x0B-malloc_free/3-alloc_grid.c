#include <stdlib.h>

int **alloc_grid(int width, int height){
    if((width <= 0) || (height <= 0)) {
        return (NULL);
    }
    int *memory = malloc(sizeof(int));
    if(memory == NULL){
        return (NULL);
    }
return (0);
}
